const SECONDS = 100;
const MINUTES = SECONDS / 60;
const HOURS = MINUTES / 60;
const DAYS = HOURS / 24;

const REFRESH_RATE = 5; //unit

module.exports = {
  SECONDS,
  MINUTES,
  HOURS,
  DAYS,
  REFRESH_RATE,
};
